.pages-emergency
  Loader(v-if='loading && !props')
  div(v-if='props')
    Hero(:props='props.hero')
    CustomEmergencyIntro(:props='props.emergency_intro')
    CustomEmergencySections(:props='props.emergency_sections')
