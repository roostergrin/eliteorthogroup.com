.pages-about-us
  Loader(v-if='loading && !props')
  div(v-if='props')
    Hero(:props='props.hero')
    Doctor(:props='props.doctor')
    Team(:props='props.team')
    Offices(:props='props.offices')  
