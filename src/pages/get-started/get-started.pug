.pages-get-started
  Loader(v-if='loading && !props')
  div(v-if='props')
    Hero(:props='props.hero')
    First(:props='props.first')  
    Divider(:props='props.divider')
    Smiles(:props='props.smiles')  
